{
  "name": "foodmine-course",
  "version": "1.0.0",
  "engines": {
    "node": ">=18 <19"
  },
  "scripts": {
    "dev": "cd frontend && npm start & cd backend && npm start",
    "start": "cd backend/built && node server.js",
    "prebuild": "cd backend && npm run build",
    "build": "cd frontend && npm run build && cp -R dist/frontend/* ../backend/built/public"
  },
  "dependencies": {
    "pg": "^8.11.5",
    "postgres": "^3.4.4"
  },
  "devDependencies": {
    "@types/pg": "^8.11.6"
  }
}

